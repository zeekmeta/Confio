# Confio 项目指引

适用于仓库根目录及全部子目录。本文维护项目边界和工作入口，开发细则由开发规范维护。

## 项目基线

- Confio 是独立的 .NET 配置读写项目，通过 NuGet 提供文件配置、模型默认值、字段保护与源生成能力。
- 已建立运行库、生成器、测试、消费者、示例和候选包验证入口，当前实现 JSON、YAML/YML、TOML、INI、普通 DI 与原生 Configuration / Options / Host 接入。1.0.0 建立首个正式 API 与文件协议基线，不保留此前内部预览版的兼容入口或双实现；正式版本的破坏性调整通过大版本处理，发布状态见更新日志。任何阶段都不得覆盖现有用户文件。
- 产品范围、两个源码项目与一个 NuGet 包的交付边界、目标框架和待验证事项由设计文档统一维护。Framework 4.6.2 至 4.8.1 分别交付 `net462`、`net47`、`net471`、`net472`、`net48`、`net481` 资产，共享源码与 API；现代运行库交付 `net8.0`、`net10.0`，.NET 9 复用 `net8.0`。AOT 验收面向 .NET 8 / 9 / 10，当前不支持 .NET 5 / 6 / 7。
- Native AOT 与真实源生成是正式能力，现代 .NET 的 JIT 与 Native AOT 消费使用同一目标资产。Framework 使用相同静态模型路径，只在实际底层 API 差异处适配，不建立反射回退。生成器属于编译阶段，其目标服务编译器加载，不进入应用运行依赖；AOT 验收覆盖完整配置路径。
- 直接创建与容器接入共享同一文件运行库，同步与异步读写均为正式能力。默认行为、首次加载、共享注册、格式转换、保护和文件生命周期由组件承担，应用声明模型与业务差异；优化时检查组件和应用的总样板与心智负担。
- 当前维护四种格式的配置读写、数据语义与可靠性，示例用于验证配置行为；通用集成深化、额外格式及更多平台适配按明确需求推进，具体边界和顺序见设计第 8.2 节。
- 文档任务不顺带创建源码或工程配置，不预建空项目、适配层和发布脚本。进入实现时按设计中的依赖顺序建立真实消费者与验证。
- 解释、技术文档和必要注释使用中文，代码符号、API、协议和库名保留原名。

## 文档入口

按任务读取相关章节，再定位实现、消费者和测试：

| 入口 | 维护内容 |
| --- | --- |
| [README](./README.md) / [简体中文](./README.zh-CN.md) | 项目简介、当前可用状态与使用入口（中英文） |
| [使用指南](./docs/使用指南.md) | 模型、读写、文件格式、保护及容器接入 |
| [开发与验证](./docs/开发与验证.md) | 构建、测试、候选包、平台验证与发布命令 |
| [设计方案](./docs/设计方案.md) | 产品契约、责任边界、消费方式、技术取舍、实施顺序与验收范围 |
| [开发规范](./开发规范.md) | 目录与项目职责、工程配置、命名与 API、数据安全、生成器与 AOT、版本和验证交付 |

发现文档与代码冲突时，先核实事实，再修改负责该信息的位置。明确区分目标设计、已实现和已实测，不以第三方宣称代替本项目证据。

## 开始工作

- 先读取本文件和相关开发规则，检查工作树；修改契约时读取设计的对应章节，并定位受影响的消费者与验证。
- 目录落点见[开发规范第 2 节](./开发规范.md#2-工程与-api)，目标与包边界见[设计第 2 节](./docs/设计方案.md#2-责任边界与交付)。实际能力与平台证据见设计第 8.1 节，不把规划目录视为已实现功能。
- 新实现按[设计第 8 节](./docs/设计方案.md#8-实施验收与待决事项)推进，第一条 JSON 可用路径带上 .NET 10、真实保护与 AOT 验证；不先完成一套反射实现再计划替换为源生成。
- 构建、行为测试、真实消费及候选包验证入口见[开发与验证](./docs/开发与验证.md)。源码消费默认使用项目引用；包与生成器交付改动执行隔离包验证。纯文档任务只运行对应文档检查。

## 参考与验证

- 参考项目及官方资料集中在设计文档中。按具体问题查阅相关说明和源码，不批量扫描其他仓库、归档、依赖缓存或日志。
- 构建与验收所需的项目文件、规则和入口由本仓库维护。
- 按开发规范选择最小有效验证。纯文档检查本地链接、交叉引用、示例和空白，不虚构尚不存在的构建命令。
- 交付说明修改、关键决定、实际验证与未验证部分；除非明确要求，不提交、推送或发布。
