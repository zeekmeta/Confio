# Confio 开发规范

适用于 Confio 源码、生成器、测试、示例与工程工具。项目边界见 [AGENTS.md](./AGENTS.md)，产品与数据契约见 [设计方案](./docs/设计方案.md)。规则生效不表示功能已经实现或验收。

“必须 / 不得”约束正确性、公开契约或责任边界；“默认 / 应”表示统一做法，有实际差异时可以调整；“可以”表示允许选项。偏离时说明理由、影响与验证，长期变化同步修订所属规则。不叠加多个模糊限定词。

## 1. 开发原则

1. 基于已知需求实现目标结构。1.0 是首个正式兼容基线，不为此前内部预览版建立过渡入口、兼容别名或双实现；正式版本的破坏性调整遵循第 5.4 节，已有用户数据始终受到保护。
2. 每项事实有一个责任方。文件状态、默认值、声明、提交与资源释放不由多个组件各自判断；具体责任以设计为准。
3. 选择简单且完整的方案。优先标准库、平台能力与成熟依赖；只有存在当前消费者、实际边界和可验证的收益时，才增加抽象、配置或扩展点。不以零继承、零包装或最少类型为目标。
4. 减少总体样板。组件承担共享注册、默认行为和生命周期接线，应用表达业务差异；不要求应用按每种格式重复模型或复制保存基础设施。
5. 在责任方修复根因。同步调整受影响的契约、调用方、测试和文档，删除失效路径，不夹带无关重构。
6. 按真实边界防御。不可信文件、密钥操作、资源交接和不可逆提交需要校验；内部已成立的不变量不层层重复检查，不以吞异常或默认值伪装成功。

技术参考应记录实际采用的做法、取舍理由与证据限制，以本项目需求确定 API、依赖和包划分。仅在存在实质取舍时写入设计，不强制为每个普通类建立决策记录。

## 2. 工程与 API

### 2.1 目录与项目职责

项目、包、目标框架和支持范围由[设计第 2 节](./docs/设计方案.md#2-责任边界与交付)维护。以下统一维护现有工程与后续能力的落点；`Formats/` 等目录在首个实际文件出现时建立，当前可用状态见 [README](./README.md)。

```text
Confio/
|-- AGENTS.md
|-- README.md
|-- README.zh-CN.md
|-- CHANGELOG.md
|-- LICENSE
|-- 开发规范.md
|-- Confio.sln
|-- Directory.Build.props
|-- Directory.Packages.props
|-- NuGet.Config
|-- .editorconfig
|-- .gitattributes
|-- .gitignore
|-- .github/
|   `-- workflows/
|       `-- release.yml
|-- docs/
|   |-- 使用指南.md
|   |-- 开发与验证.md
|   `-- 设计方案.md
|-- src/
|   |-- Confio/
|   |   |-- Confio.csproj
|   |   |-- Declarations/
|   |   |-- Formats/
|   |   |-- Protection/
|   |   |-- Integration/
|   |   `-- Internal/
|   `-- ConfioGenerator/
|       `-- ConfioGenerator.csproj
|-- tests/
|   |-- ConfioTests/
|   |-- ConfioConsumer/
|   `-- ConfioConsumerModels/
|-- samples/
|   `-- ConfioSample/
|-- eng/
`-- artifacts/
```

| 位置 | 职责与边界 |
| --- | --- |
| `src/Confio/` 根目录 | 文件与类型读写的主要公开入口；具体 API 名称由设计维护 |
| `Declarations/` | 配置特性、静态声明契约与元数据关联；不引用 Roslyn |
| `Formats/` | JSON、YAML、TOML、INI 的原生节点保留、格式与模型节点适配及可表达性校验，不分别绑定应用模型 |
| `Protection/` | 保护契约、密文处理与系统密钥设施接入；待保护数据由应用声明 |
| `Integration/` | 原生 Configuration、Options、DI 与 Host 接入，复用文件实例而非另建运行时 |
| `Internal/` | 文件提交、快照及共享协调实现，按具体职责命名，不建立通用工具平台 |
| `src/ConfioGenerator/` | 增量生成入口、声明分析、诊断与代码输出；初期保持浅层，文件增加后按实际职责分目录 |
| `tests/ConfioTests/` | 运行库行为与生成器测试；按被测行为组织，不另建公共测试基础包 |
| `tests/ConfioConsumer/` | 有明确成功断言的真实消费程序，用于目标框架、包、生成器与 AOT 验收，不承担测试 runner 的职责 |
| `tests/ConfioConsumerModels/` | 由真实消费者引用的多目标模型类库，验证模型声明和生成结果跨项目交付 |
| `samples/ConfioSample/` | Avalonia 交互式功能示例，展示直接读写、多模型与保护、普通 DI、原生读取与 Host；提供桌面按钮验收、截图和 AOT 消费入口，UI 依赖仅由该示例引用，不代写组件基础设施 |
| `eng/` | 需要重复执行的组合验证或打包脚本，完整命令由开发与验证文档维护，不再平行建立 `scripts/`、`tools/` |
| `.github/workflows/` | 调用仓库现有构建与验收入口，按版本标签发布已经验证的同一份 NuGet 包；发布凭据由受信发布或仓库机密提供 |
| `artifacts/` | 候选包、发布产物和测试报告等可再生成输出，不提交 Git |

源码和工具目录随首个实际文件建立，不创建空目录或占位类型。JIT 与 AOT 优先复用同一消费者，不机械地按格式或发布方式复制项目；实际隔离需要按[第 6.2 节](#62-真实消费者与候选包)处理。行为与生成器测试使用同一种测试框架，运行环境验收由真实消费者承担。

目录是代码组织，不自动形成公开 namespace、接口层或独立包。没有当前消费或依赖隔离收益时，不建立 `ConfioCore`、`ConfioAbstractions`、`ConfioAot` 等额外项目或包，也不为每个格式拆包。

### 2.2 工程配置与引用

- 仓库使用一个解决方案 `Confio.sln`，各 `.csproj` 使用 SDK 风格，不同时维护另一份解决方案或恢复旧式项目模板。
- `Directory.Build.props` 维护共享编译规则、nullable、语言版本、确定性构建和统一版本来源；各项目维护自己的目标框架、引用和打包职责，不把运行库目标无条件传给生成器、测试和示例。
- `Directory.Packages.props` 是依赖版本的唯一来源。仅在需要共享构建目标或后置配置时增加 `Directory.Build.targets`，不为对称性预建。`NuGet.Config`、CI、发布脚本和 `global.json` 在真实需求出现时建立，不写维护者机器专属路径或凭据。
- 开发 SDK、生成器宿主的 Roslyn、消费者 C# 与应用运行时分别记录，明确相对项目默认值所需的编译设置。使用受支持的稳定工具链和明确语言版本，不使用 `latest` 或 `preview` 暗中提高消费门槛，也不以调高语言版本代替验证；源生成输出遵守已声明的消费者语言与 API 范围。
- 运行库不形成对 Roslyn 或生成器的运行时引用。打包可通过仅构建的项目引用安排生成器构建，不把它变成运行依赖；生成器通过编译符号分析 Confio 声明，不加载运行库执行应用代码，也不反向依赖运行库项目形成循环。
- 测试和示例不被正式源码引用。内部访问确有必要时使用局限于测试的 `InternalsVisibleTo`，不为测试扩大公开 API。
- 运行库仅构建当前支持目标，不预留旧运行时的资产、兼容依赖或条件编译分支。新增目标须先明确消费需求和验收环境；确有平台 API 差异时在具体操作处处理，不复制整个运行时。

### 2.3 命名与文本格式

- 项目和程序集名称使用不带点分前缀的 `PascalCase`，目录与项目文件同名。运行库公开入口默认使用根 namespace `Confio`，生成器使用 `ConfioGenerator`；内部 namespace 可按职责细分，不要求消费者因实现目录而增加多个 `using`。
- C# 使用四空格缩进和 nullable 引用类型，仓库手写源码采用文件范围 namespace；生成代码和消费示例遵守已声明的消费者语言下限，不套用会提高门槛的语法。默认一个主要类型一个文件，小型私有辅助类型可以嵌套。
- 类型和成员使用 `PascalCase`，接口以 `I` 开头，参数和局部变量使用 `camelCase`，私有实例字段使用 `_camelCase`。
- 异步方法以 `Async` 结尾，公开异步读写使用 `Task` / `Task<T>`，`CancellationToken` 放在最后并提供默认值。机器协议使用 `InvariantCulture`，文件路径通过 `System.IO.Path` 等原生 API 构造。
- 手写文本使用 UTF-8、LF；供 Windows PowerShell 5.1 执行的脚本使用 UTF-8 BOM。工程建立时由 `.editorconfig` 落实缩进、编码、末尾换行与空白，由 `.gitattributes` 落实 Git 文本换行；这些是工具规则，不作为可随意偏离的风格偏好。
- `.gitignore` 忽略 `bin/`、`obj/`、`artifacts/`、IDE 本机状态和工具生成文件。测试使用虚构数据；真实配置、密钥、凭据、候选包与发布输出不进入源码历史。
- 只整理本次修改的内容，不顺带格式化、重命名或升级无关文件。

### 2.4 公开契约与注册

公开类型必须有应用或扩展作者直接使用的理由，否则使用 `internal`。公开契约说明输入、结果、取消、失败及必要的线程与所有权限制，类型不为测试方便公开。

通用集成使用原生 DI、Configuration、Options 和 Host，普通 `IServiceCollection` 消费不要求 Host。普通注册返回原生服务集合，注册与 Confio 服务构造不做文件或密钥 I/O，首次明确读写负责加载；Configuration / Options 的专用 Add 接入负责预加载，随后由应用正常调用原生 Build。不建立第二个根容器、全局服务定位器或专用启动 Builder，不为提前解析服务调用 `BuildServiceProvider()`。直接创建、类型服务和宿主消费共享同一文件状态与读写实现。

沿用原生 `Add`、`Configure`、`Use` 等入口语义，不为统一命名增加同义包装。可替换的单一默认服务使用 `TryAdd*`，不得覆盖已有显式注册；内部必需服务与集合贡献按实际契约注册。每个可重复入口说明合并、覆盖或拒绝语义，不用 `TryAdd` 隐藏配置节归属冲突。

普通 DI 的单次注册、上下文集合、单例共享和外部实例重载遵守[设计第 4.5 节](./docs/设计方案.md#45-普通容器注册)。生成声明使用已知模型的封闭泛型注册，运行库集中完成服务接线，不生成一套独立注册实现。默认类型服务不增加缓存、锁、可释放资源或作用域依赖，也不将可变配置模型直接注册为共享服务；应用不逐模型绑定或复制工厂。选择原生读取接入时，按[设计第 4.4 节](./docs/设计方案.md#44-与-configurationoptions-的关系)接入 Configuration / Options，保留原生配置源优先级、对象缓存和校验时序。

配置项必须有责任方、默认值、生效时机和无效时行为。固定协议与内部时序留在实现中，默认行为不要求应用额外配置。

模型持久化校验、`Action<T>` / `Func<T,T>` 更新和直接订阅遵守[设计第 4 节](./docs/设计方案.md#4-读写-api提交与原生读取视图)。直接使用与 DI 在同一候选快照边界执行业务规则；类型服务不重复校验，不让应用注册同一规则的多个适配器。直接订阅复用文件变化令牌，不增加事件总线、属性差异缓存或 UI 依赖。

## 3. 注释与诊断

公开类型和成员使用中文 XML 文档，说明职责和签名不能表达的限制。`<summary>`、正文和 `</summary>` 分行；接口实现优先使用 `<inheritdoc />`。internal/private 只注释不明显的生命周期、线程、所有权、失败边界或实现意图，不复述名称和普通赋值。

测试与示例按可观察行为命名及教学需要注释，不要求每个测试方法都有 XML 文档，不手工编辑生成代码。注释描述当前契约，不记录讨论流水。

开发者异常、日志模板和生成器诊断消息使用英文，保留安全且必要的上下文。异常文本不是稳定协议，也不直接作为最终用户文案；组件不加入 UI 文案或本地化框架。不得输出密码、密钥、token 或完整敏感配置正文。

## 4. 生命周期与数据安全

- 一个最终责任方释放资源。普通 DI 的文件由根容器创建和释放；已有实例始终借用。原生 Add 预加载路径在交接前负责自建文件，失败时清理；接入后文件归配置源管理，随 ConfigurationManager 或 Host 释放，不依赖业务将来解析服务才取得所有权。
- 原生集成不接管整个容器或 Host 的 Build，不捕获 IConfiguration 工厂，也不清理应用的其他 Provider。配置根按原生生命周期管理，普通消费用 `using`，已有独立根通过原生配置源明确组合。保留已有显式 IConfiguration 注册和应用选择的容器工厂，不靠临时容器或反射获取 Host 内部字段。
- 文件流和单次操作取得的资源在该操作作用域内清理；包含 `IAsyncDisposable` 的资源沿可等待路径释放。调用方在操作全部完成后释放文件实例，`Dispose` 只清理实例拥有的同步资源，不隐式保存或等待尚未完成的异步操作。只有实际资源需要异步释放时才增加对应的公开生命周期契约。
- 文件实例构造不做文件或密钥 I/O。首次读写的加载由组件承担，已加载后的读取只访问快照；默认加载发现声明位置的非空明文时，校验候选后沿文件提交路径自动保护写回，不补写缺失默认值。应用可明确关闭加载回写，显式写入仍执行保护。原生预加载和刷新复用同一路径，不新增初始化协议，也不在同步 DI 工厂、Options 工厂或业务构造函数中阻塞异步加载。
- 同步与异步入口共享状态和纯处理规则，I/O 与协调等待使用各自路径。不用 `.Wait()`、`.Result` 或 `GetAwaiter().GetResult()` 阻塞异步路径，也不用 `Task.Run` 包装同步读写。原生平台调用的阻塞与取消边界如实记录，不把 CPU 计算或文件提交全部包装成异步。
- 事件、回调和实际需要的后台任务有退订、停止或等待位置，不因发出取消就宣称操作已结束；默认不为配置保存建立后台任务。
- 无线程亲和要求的类库内部异步等待默认使用 `ConfigureAwait(false)`；有明确线程要求的边界按契约处理，不机械套用。回调与通知的线程限制写入公开契约，不假定存在 UI 上下文。
- 长期 I/O 传递取消令牌，提交前与提交后的取消遵守同一数据契约。失败或迟到取消不能把已经提交的保存报告成回滚。
- 保存只替换目标模型拥有的配置部分，其他所选声明位置的明文保护合并为同一次提交，保留非目标配置节的数据及已有密文；保存与重载的协调、提交点、快照发布与通知遵守[设计第 4 节](./docs/设计方案.md#4-读写-api提交与原生读取视图)，不另建通用事务框架。
- 文件、格式、声明与密文在所属输入边界校验。缺失值、显式空值、损坏文件和解密失败是不同情况，不静默覆盖源文件或退回默认值。
- 使用成熟的认证加密和密钥保护设施，不自研密码算法。保护失败不得降级为明文，配置文件不存放自己的解密密钥。
- 密文识别只针对文件输入中所选声明的保护位置，文件前缀与错误处理遵守[设计第 6.1 节](./docs/设计方案.md#61-保护语义)。模型输入始终按明文处理，不以字符串前缀跳过加密，也不把解密结果再次识别为密文。
- 所有创建和注册入口统一使用 `ConfigurationFileOptions`，普通应用不创建 AES 保护器、编写密钥生成持久化代码或重复平台接线；外部保护器始终借用。自动密钥首次生成复用文件锁与原子提交，持久化成功后才能提交密文，解密缺钥不新建替代密钥。密钥副本和资源释放遵守[设计第 6.2 节](./docs/设计方案.md#62-密钥默认与替换)，静态声明元数据不持有可释放的保护状态。
- 安装目录默认只读。文件及密钥路径在入口固定，程序集简单名称仅帮助选择默认位置，不进入加密认证用途；密钥来源、字段位置与格式语义各由所属边界负责。

参数错误、非法状态和取消优先使用语义准确的 BCL 异常；只有调用方确需按类型区分时才增加领域异常。`catch` 必须承担恢复、清理、安全诊断或补充上下文后传播的职责，不保留仅重新抛出的包装。同一失败不逐层重复记录，清理错误不覆盖原始故障。

## 5. 依赖、生成器与 AOT

### 5.1 依赖与目标框架

- 外部 NuGet 依赖通过根 `Directory.Packages.props` 集中管理明确版本，`PackageReference` 不另写 `Version` 或 `VersionOverride`，不使用浮动版本。目标框架已提供所需 API 时直接使用 SDK 资产；旧目标确实需要更高版本的静态元数据 API 时，按目标引用官方兼容包，不复制模型转换实现。
- 新依赖和升级必须有当前用途，核实许可、目标框架、体积、平台与 AOT 影响。第三方介绍、示例或旧版本测试不能证明本项目组合路径可用。
- .NET 10 的 JIT 与 Native AOT 共享静态模型转换和完整配置语义，使用同一运行库资产，不按发布方式切换序列化机制或维护两套配置行为。
- 当前目标矩阵由设计第 2.1 节维护，之外的运行时按明确需求评估。扩展时核实完整依赖链、平台设施和消费成本；语言辅助类型优先使用编译期依赖，必要时随包传递，应用不手写辅助源码。不以反射降级、拆包或应用样板掩盖大量分叉。

### 5.2 源生成与交付

- 使用 Roslyn `IIncrementalGenerator`，输入只来自显式声明与当前编译。生成过程不读取真实配置、密钥、网络或机器路径，不以静态可变状态跨编译缓存应用信息；按生成器取消令牌停止工作。
- 生成器减少已经成立的显式代码，手写声明与生成结果调用同一正式 API，由运行库统一完成类型服务及原生 Options 接线，不在生成器中复制注册流程。生成器只处理显式声明，不引入运行时扫描或全局初始化。
- 静态模型转换复用原生 JSON 元数据，配置生成器只负责所属声明、元数据引用、默认构造、保护映射与诊断，不重复生成模型转换器或完整结构元数据。两个生成器读取同一手写上下文，不依赖普通生成器之间的输出串联，不增加两阶段构建。
- 模型与上下文限制按设计在编译期或文件实例构造、注册边界检查；最终元数据确定后固定结果。文件和保存值在各自输入边界校验，不把同一声明检查复制到每次读取、每种格式或每个调用层。
- 模型默认构造使用生成的静态调用，初始化值继续由模型声明。补齐 `init` 缺失值时复用共享节点规范化，保留显式空值、必填成员检查和嵌套替换语义，不生成第二套序列化器或默认值表。
- 输出使用稳定且不冲突的文件名、确定的顺序和明确类型引用，不包含时间戳、绝对路径或随机标识。错误定位到对应声明，诊断 ID 稳定。生成代码由编译器管理；需要落盘检查时只写入 `obj/` 等中间目录，不默认要求落盘，也不加入源码目录或手工维护。
- 正式包在 `lib/<TFM>/` 提供运行库，在 `analyzers/dotnet/cs/` 提供生成器；生成器构建依赖不泄漏为应用运行依赖。生成器确需私有运行依赖时按分析器加载规则随附，不把 Roslyn 宿主程序集作为应用依赖交付。
- 验证真实输出编译、诊断、生成器加载、重复构建、多上下文、多项目消费及手写等价性，不能只比较输出字符串或测试接口替身。候选包消费同时验证原生 JSON 生成器和 Confio 生成器可用，不能只验证源码引用。消费者不需要另装一个生成器包，也不需要手工声明逐模型注册清单。

### 5.3 Native AOT 门禁

- 第一条 JSON 正式路径即验证真实生成、同步与异步文件读写、保护和系统密钥的 AOT 行为，新增格式与原生接入随实现补充验证，不等功能完成后再补。使用显式泛型、delegate、`JsonTypeInfo<T>` / `JsonSerializerContext` 与可静态分析的接线，不依赖运行时扫描业务模型、动态代码生成或无静态元数据的反射绑定。
- 运行库及真实消费路径的 .NET 8 / 9 / 10 目标启用 `IsAotCompatible` 和 AOT、裁剪分析，Framework 和生成器不作为 AOT 发布目标。实际消费者通过 `PublishAot` 发布，NuGet 仍交付可被应用编译的托管库，不为此另发一套预编译本机配置库。
- 门禁覆盖格式适配、模型转换、保护、系统密钥设施和原生 DI / Configuration / Options 接入，不只验证 JSON 序列化。禁止依靠应用批量保留程序集、追加逐模型反射豁免或自己补共享接线来兑现组件承诺。
- AOT / trim warning 必须修复，或在所属设计中说明来源、受影响路径、接受理由与验证边界；不能通过全面抑制制造通过。
- 编译器、SDK 和运行时要求分别说明。生成器的 `netstandard2.0` 目标只服务编译器加载，不扩展应用运行时的支持范围；Native AOT 按实际交付的 OS / 架构发布并运行。

### 5.4 版本与发布

首个正式版本为 `1.0.0`，版本统一由 `Directory.Build.props` 维护。正式发布采用语义化版本：同一主版本保持公开 API 与持久化格式兼容，新增兼容能力递增次版本，兼容修复递增修订版本；破坏性调整递增主版本并说明影响和处理方式。预览版本使用明确的预发布标识，不要求 1.0 兼容此前内部预览版。运行库各目标与生成器随同一包版本交付，不各自维护发布序列；同一包 ID 的已发布版本不覆盖。

正式发布前核实包名、许可证、仓库地址、包说明和目标矩阵，不虚构许可证或远端地址。不借版本整理升级无关依赖，不为设计阶段预建发布流水线或固定 SDK 补丁版本。

## 6. 验证与交付

### 6.1 按影响验证

| 变更 | 最小有效验证 |
| --- | --- |
| 文档与规范 | 本地链接、交叉引用、示例一致性、空白与换行；修改图示时验证实际解析或渲染 |
| 局部实现 | 受影响项目构建、必要行为测试和直接消费者 |
| 公共契约、文件提交或保护 | 相关消费者、失败与取消路径、数据保留和敏感信息泄漏检查；并发变更验证实际多进程行为 |
| 格式、生成器或 AOT | 真实产物和消费路径，受影响目标平台上的发布及运行 |
| 包与发布 | 隔离 NuGet 消费、资产选择、分析器交付及版本来源检查 |

测试覆盖可观察结果，不镜像私有实现。文件和进程测试使用隔离目录、专用密钥文件或测试密钥，以及外层期限，不接触真实用户配置或删除用户密钥。用可控信号代替固定 sleep，不为低影响改动建立大型测试基础设施。

### 6.2 真实消费者与候选包

开发测试、消费者与示例默认通过 `ProjectReference` 消费当前源码，同时验证调用成本。相同场景优先复用现有消费者或示例的消费逻辑；确有构建或运行隔离需要时，可以建立独立最小消费者，不强制 AOT、多进程及多项目生成场景共用一个程序。

包验收在隔离目录中建立只含候选包 `PackageReference` 的消费工程，使用独立 feed 和缓存核实实际解析来源；不能同时保留源码直引。

真实消费者需要显式调用并断言本次验收的行为，失败返回非零退出码；测试 runner 不直接当作 AOT 程序发布。读写验收包含同步与异步调用，保护验收使用非空受保护值及真实平台设施，不能以未执行保护路径的示例代替。包消费同时检查运行库目标选择、对应依赖组、分析器加载和运行输出中没有 Roslyn / 生成器程序集。

DI 验收使用最终原生容器，覆盖多文件的模型归属、跨作用域共享、首次业务操作加载、重复注册与显式替换、自建和外部实例释放；原生接入另验证配置源组合、Options 缓存及校验、预加载早于消费者构造、预加载失败清理及配置根所有权。使用不同已知值验证文件快照、组合根和 Options 缓存各自的数据来源与刷新规则。相关验证进入 JIT / AOT 的实际消费路径，不以手工注册或容器替身代替生成声明接入。

完整交付按[设计第 2 节](./docs/设计方案.md#2-责任边界与交付)确认的目标矩阵和[第 8 节](./docs/设计方案.md#8-实施验收与待决事项)的行为条件验收，Framework、现代 .NET JIT 与 Native AOT 分别取得实际运行证据。记录 Framework 实际宿主版本，较高版本上的运行不等于较低版本原始宿主已实测。新增运行时或平台时同步增加对应消费和验收环境，不把仅能构建、NuGet 可解析或其他平台通过当作运行证明。

### 6.3 记录与提交

具体命令在真实工程建立并执行后写入开发与验证文档或相应工程说明，README 保留常用入口。单次命令与结果记录在交付说明、PR 或 CI 中，长期支持范围与未验收边界由设计文档维护。本机构建或交叉发布不能证明其他平台已经运行；环境不足时说明未运行范围，不虚构通过。

交付区分通过、失败、跳过和未运行，说明修改、关键决定、实际执行的验证及剩余风险。无新变更或疑点时结束验证，不重复全仓门禁，也不删除测试或扩大 warning 豁免来制造通过。

提交按完整意图组织，默认标题为 `type: 简短中文说明`，按内容使用 `feat`、`fix`、`refactor`、`docs`、`test`、`build` 或 `chore`，不强制 scope 或为每个文件单独提交。不夹带无关修改，必要时在正文说明破坏性变化与验证。提交、推送和发布需有明确请求。

## 7. 文档维护

- 根目录 `README.md`（英文）和 `README.zh-CN.md`（中文）提供内容一致的项目介绍、快速开始与使用入口，变更时同步维护；两份均随 NuGet 包交付，仓库与包页面默认显示英文。使用指南维护详细用法，Sample 目录的 README 维护交互演示，开发与验证文档维护完整命令与发布流程。AGENTS.md 提供边界与工作入口，本文维护工程规则，设计方案维护产品契约、技术取舍和验收范围，CHANGELOG 记录面向使用者的版本变更。同一事实修改其责任文档，其他位置引用。
- 设计草案、参考事实、源码实现和实际验收分别表述。尚未实现的示例明确标注，不把候选依赖或目标平台写成已验证能力。
- 产品定位和责任边界直接描述 Confio 自身的能力、约束与应用职责，技术参考用于说明实际采用的依赖或取舍。
- 设计说明保留问题、决定、理由、代价与验证条件。普通类和方法的实现选择留在源码与测试，不为每次改动新增方案文档。
- 本地文档使用仓库内可解析的相对链接。中英文根 README 同时用于仓库与 NuGet 包，跨页及语言切换链接使用项目仓库的绝对 URL，不另建包说明副本。外部参考使用明确来源，并说明采用内容与证据限制。
- 长期文档不保存聊天流水、平行版本或测试计数；不保留第二份完整契约作为迁移副本。
